export enum UserRole {
    CUSTOMER = 'customer',
    WORKER = 'worker',
    ADMIN = 'admin'   
}

export enum UserLoginMethod {
    GoogleAuthentication = 'googleauthentication',
    Email = 'email'
}